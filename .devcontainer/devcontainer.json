{
    "name": "DinD and Azure CLI",
    "dockerFile": "Dockerfile",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/sh"
    },
    "runArgs": [
        "--privileged"  // This is required for Docker-in-Docker
    ],
    "extensions": [],
    "postCreateCommand": "service docker start",
    "forwardPorts": []
}
